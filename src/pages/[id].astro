---
import Layout from "../layouts/Layout.astro"
import Main from "../components/Main.svelte"

const { id } = Astro.params
const menu: Menu = await (
  await fetch(`http://localhost:4321/menus/${id}.json`)
).json()
---

<Layout title="menupi">
  <Main menu={menu} client:only="svelte" />
</Layout>
